{
  "name": "coder-core",
  "version": "0.0.0",
  "private": true,
  "description": "coder shared foundation: client<->server protocol, domain types, event-log, worktree+git glue, registry loaders. Depends on nothing in-repo.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "echo 'coder-core is source-only; consumers bundle it'",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "lint": "echo 'no linter configured yet'"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/node": "^22",
    "typescript": "^5"
  },
  "license": "MIT"
}
