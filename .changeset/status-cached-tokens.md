---
"@glrs-dev/aj": patch
---

Status line: replace the `(NN%⚡)` cache marker on the ctx counter with a `cached {tokens}({share}%)` stat next to the input counter — the latest request's provider-cache read tokens and their share of that request's input.
