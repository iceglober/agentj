---
"@glrs-dev/aj": patch
---

Installed interactive sessions now check for updates after the TUI starts and notify instead of auto-installing or restarting. The `update.auto` setting controls the check; `/update` remains explicit.
