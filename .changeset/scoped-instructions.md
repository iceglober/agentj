---
"@glrs-dev/aj": minor
---

Projects can configure scoped Markdown instruction extensions, such as a plan-only architecture checklist in `.aj/extensions/plan.md`, and use typed `agentj.ts` configuration.
