---
"@glrs-dev/aj": minor
---

Add a `/release` skill that finds the open Changesets `Version Packages` pull request, safely squash-merges its recorded revision, and reports the released package versions and generated changelog; for example, it replies that nothing needs releasing when no such pull request is open.
