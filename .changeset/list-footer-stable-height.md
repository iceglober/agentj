---
"@glrs-dev/aj": patch
---

Windowed lists (slash completions, guided choices) replace the separate `… ↑ N more`/`… ↓ N more` marker rows with one always-present footer row (`… ↑ 3 · ↓ 12 more`), so the menu no longer changes height and jumps as markers appear and disappear while scrolling.
