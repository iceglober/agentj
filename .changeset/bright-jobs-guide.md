---
"@glrs-dev/aj": minor
---

Add the built-in `running-background-work` skill so aj can guide long-running work such as waiting for CI or deploying: it starts an appropriate background job instead of claiming that it will wait in the foreground.
