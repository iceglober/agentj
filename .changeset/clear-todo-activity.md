---
"@glrs-dev/aj": patch
---

Keep the persistent Todos panel visually separate from live tool activity, with clear completed, active, and pending markers. For example, several active tasks now each use an arrow and are counted in the panel header instead of blending into a running tool spinner.
