---
"@glrs-dev/aj": patch
---

Split the status footer beneath the composer into two lines: location · model · context on the first, the `Tab mode · / commands` controls on the second. The location now uses the full width before it is shortened, so it stays readable on narrower terminals. Applies to both interfaces.
