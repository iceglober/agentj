---
"@glrs-dev/aj": patch
---

Skills can set top-level `user-invocable: false` in SKILL.md frontmatter to remain available to the model without registering a slash command. The bundled `running-background-work` skill now uses this setting.
