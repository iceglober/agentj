---
"@glrs-dev/aj": patch
---

Give the "Esc interrupt" affordance an ambient busy animation: a small VU-style meter of block bars (▁▃▅▇) hums to its left while the agent is working, so a running turn reads as alive rather than frozen. Driven by the existing status ticker; still one line, still both interfaces.
