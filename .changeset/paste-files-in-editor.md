---
"@glrs-dev/aj": minor
---

You can now press Ctrl+V in the prompt editor to insert copied local files as editable `@file` references. Paths with spaces are quoted automatically, so copying a file such as `notes/release plan.md` attaches it when you submit the prompt.
