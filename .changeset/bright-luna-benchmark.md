---
"@glrs-dev/aj": minor
---

Raise the default GPT-5.6 Luna reasoning effort to the externally validated medium setting, persist token usage for `agentj run`, and add a reproducible cross-agent SWE-bench benchmark with models.dev cost estimates.
