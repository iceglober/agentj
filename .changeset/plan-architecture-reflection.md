---
"@glrs-dev/aj": minor
---

Plan-mode reflections now ask agents to describe how a proposed change will leave the code simpler, including which existing abstractions they will reuse, extend, remove, or create. For example, a refactor plan now identifies the shared boundary it will use instead of adding another duplicate helper.
