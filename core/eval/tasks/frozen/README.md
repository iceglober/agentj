# Frozen tasks

The held-out regression split used for release comparisons. `baseline.ts`
selects versioned, self-checked tasks across editing, synthesis, ambiguity,
delegation, and mocked operational work. Change a promoted task's version when
its contract changes so historical results remain distinguishable.
