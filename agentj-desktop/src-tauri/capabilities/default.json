{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Core capability for the main window: event listen/emit for the agent stream.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
