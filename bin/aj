#!/usr/bin/env bun
// aj — short alias for the agentj binary entry point.
import { main } from "agentj";

main(process.argv.slice(2)).catch((err) => {
  console.error(err instanceof Error ? err.stack ?? err.message : String(err));
  process.exit(1);
});
